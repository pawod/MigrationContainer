namespace DataMigratorTest.Normalization
{
    using Microsoft.VisualStudio.TestTools.UnitTesting;

    [TestClass]
    public class NormalizationTest
    {
        [TestMethod]
        public void AttributesTest()
        {
            //            var normalizer = new NtfsNormalizer();
            //            var sourceFile = TestHelper.TestDir.GetRandomFile();
            //            var dateTimes = normalizer.NormalizeDateTimeAttributes(sourceFile);
            //
            //            new DateTime(dateTimes["CreationTimeUtc"]).Should().Be(sourceFile.CreationTimeUtc);
            //            new DateTime(dateTimes["LastAccessTimeUtc"]).Should().Be(sourceFile.LastAccessTimeUtc);
            //            new DateTime(dateTimes["LastWriteTimeUtc"]).Should().Be(sourceFile.LastWriteTimeUtc);
        }
    }
}
